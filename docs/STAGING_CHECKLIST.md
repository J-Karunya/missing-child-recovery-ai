# Staging checklist

Verify login, Argon2 passwords, MFA, session expiry/logout, parent isolation, reviewer/station permissions, encrypted evidence access, scanner fail-closed behavior, legal holds, audit logs, backup/restore to a temporary database, HTTPS reverse-proxy configuration, notifications, and the rule that AI results remain PENDING pending human review.
