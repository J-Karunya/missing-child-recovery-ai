# Reverse-proxy deployment example

This folder is documentation only. The local Streamlit prototype is not an HTTPS server. In a future approved deployment, terminate TLS at a maintained reverse proxy, restrict Streamlit/database/evidence storage to private networks, inject secrets through a managed provider, and keep backups encrypted and access-controlled.
